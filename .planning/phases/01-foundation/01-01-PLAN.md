---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - postcss.config.mjs
  - .gitignore
  - .eslintrc.json
  - .prettierrc
  - src/lib/types.ts
  - src/lib/planDefaults.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/components/AppShell.tsx
  - src/components/TopNav.tsx
  - src/components/Sidebar.tsx
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03

must_haves:
  truths:
    - "Running `npm run dev` starts the Next.js dev server on localhost:3000 with no TypeScript or build errors"
    - "A `FinancialPlan` TypeScript interface exists covering all data categories: income/expenses, assets/liabilities, goals (retirement, purchases, education, legacy), risk tolerance, and simulation results placeholder"
    - "All sub-types (Goal, RetirementGoal, PurchaseGoal, EducationGoal, LegacyGoal, Assets, Liabilities, SimulationResults) are exported from src/lib/types.ts"
    - "Visiting localhost:3000 renders a page with a top navigation bar, a sidebar placeholder, and a main content area"
    - "The app compiles cleanly with `npm run build` (no TypeScript errors)"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "FinancialPlan TypeScript interface and all sub-types"
      exports: ["FinancialPlan", "Goal", "RetirementGoal", "PurchaseGoal", "EducationGoal", "LegacyGoal", "Assets", "Liabilities", "RiskTolerance", "SimulationResults", "GoalResult"]
    - path: "src/lib/planDefaults.ts"
      provides: "createEmptyPlan() factory function returning a valid empty FinancialPlan"
      exports: ["createEmptyPlan"]
    - path: "src/app/layout.tsx"
      provides: "Root Next.js App Router layout wrapping every page with AppShell"
    - path: "src/components/AppShell.tsx"
      provides: "Full-page layout: TopNav + Sidebar + main content slot"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/AppShell.tsx"
      via: "import and render as children wrapper"
    - from: "src/app/page.tsx"
      to: "src/app/layout.tsx"
      via: "Next.js App Router page slot"
---

<objective>
Bootstrap the Next.js application and define the complete TypeScript data model for a financial plan.

Purpose: Every subsequent phase reads from and writes to the `FinancialPlan` type. Defining it completely in Phase 1 means Phases 2-4 fill in fields without reshaping the schema. The app shell establishes the layout structure that later phases slot UI into.

Output: A running Next.js app with Tailwind, a full `FinancialPlan` type hierarchy in `src/lib/types.ts`, a `createEmptyPlan()` factory, and an app shell with top nav, sidebar, and a dashboard placeholder page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md

<interfaces>
<!-- No prior code — this plan establishes all foundational interfaces. -->
<!-- The FinancialPlan type must cover ALL fields needed by Phases 2, 3, and 4: -->
<!-- Phase 2 (Interview): income, expenses, assets, liabilities, goals, riskTolerance -->
<!-- Phase 3 (Simulation): reads the full plan, writes simulation results per goal + overall -->
<!-- Phase 4 (Results): reads simulationResults for display and PDF -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project with Tailwind and project config</name>
  <files>
    package.json, tsconfig.json, next.config.ts, tailwind.config.ts,
    postcss.config.mjs, .gitignore, .eslintrc.json, .prettierrc,
    src/app/globals.css
  </files>
  <read_first>
    - /home/john/Projects/MonteCarloFinancialPlanning/.planning/phases/01-foundation/01-CONTEXT.md (D-01 through D-09 decisions)
    - /home/john/Projects/MonteCarloFinancialPlanning/.planning/REQUIREMENTS.md (full requirements for context)
  </read_first>
  <action>
Run the following command from the project root `/home/john/Projects/MonteCarloFinancialPlanning` to scaffold the Next.js app with all required options:

```
npx create-next-app@latest . \
  --typescript \
  --tailwind \
  --eslint \
  --app \
  --no-src-dir \
  --import-alias "@/*"
```

Wait — the CONTEXT.md decisions specify using a `src/` directory layout for clarity. After scaffolding, if `create-next-app` put files directly under `app/`, reorganize into `src/app/`, `src/lib/`, `src/components/` manually.

Alternatively, run with `--src-dir` flag:

```
npx create-next-app@latest . \
  --typescript \
  --tailwind \
  --eslint \
  --app \
  --src-dir \
  --import-alias "@/*"
```

After scaffolding:

1. Add `.prettierrc` at project root with content:
   ```json
   {
     "semi": true,
     "singleQuote": true,
     "trailingComma": "es5",
     "printWidth": 100,
     "tabWidth": 2
   }
   ```

2. Add `prettier` and `prettier-plugin-tailwindcss` as dev dependencies:
   ```
   npm install --save-dev prettier prettier-plugin-tailwindcss
   ```

3. Update `.prettierrc` to include the Tailwind plugin:
   ```json
   {
     "semi": true,
     "singleQuote": true,
     "trailingComma": "es5",
     "printWidth": 100,
     "tabWidth": 2,
     "plugins": ["prettier-plugin-tailwindcss"]
   }
   ```

4. Ensure `data/` directory is in `.gitignore` (add `data/` line to prevent committing plan.json with sensitive financial data). Also add `.env.local` if not already present.

5. Remove the default Next.js boilerplate from `src/app/page.tsx` — replace with a simple placeholder (see Task 3).

6. Remove default boilerplate from `src/app/globals.css` — keep only Tailwind directives:
   ```css
   @tailwind base;
   @tailwind components;
   @tailwind utilities;
   ```

7. Confirm `tsconfig.json` has `"strict": true` and `"paths": { "@/*": ["./src/*"] }`.
  </action>
  <verify>
    <automated>cd /home/john/Projects/MonteCarloFinancialPlanning && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists and contains `"next"`, `"react"`, `"typescript"`, `"tailwindcss"`, `"prettier"` as dependencies
    - `tsconfig.json` contains `"strict": true`
    - `.gitignore` contains `data/` on its own line
    - `src/app/globals.css` contains `@tailwind base` and does NOT contain `font-family` or other default Next.js boilerplate
    - `.prettierrc` exists and contains `"singleQuote": true`
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>Next.js project scaffolded with TypeScript strict mode, Tailwind CSS, ESLint, Prettier, and src/ directory layout. Build passes with no errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define FinancialPlan TypeScript type hierarchy</name>
  <files>src/lib/types.ts, src/lib/planDefaults.ts</files>
  <read_first>
    - /home/john/Projects/MonteCarloFinancialPlanning/.planning/REQUIREMENTS.md (INT-02 through INT-05 for all fields needed by Phase 2; SIM-02, SIM-03 for simulation result shape; RSLT-02 for goal breakdown shape)
    - /home/john/Projects/MonteCarloFinancialPlanning/.planning/phases/01-foundation/01-CONTEXT.md (D-04, D-05 decisions on schema completeness and TypeScript types)
  </read_first>
  <behavior>
    - `createEmptyPlan()` returns an object that satisfies the `FinancialPlan` interface with no undefined fields — all arrays are `[]`, all numbers are `0`, all strings are `''`, all nested objects are present with their own zero values
    - `createEmptyPlan().income.salary` is `0` (number, not undefined)
    - `createEmptyPlan().goals` is `[]` (array, not undefined)
    - `createEmptyPlan().simulationResults` is `null` (simulation not yet run)
    - `createEmptyPlan().riskTolerance.score` is `0`
    - `createEmptyPlan().metadata.createdAt` is a non-empty ISO string
    - TypeScript: assigning a string to `FinancialPlan.income.salary` is a type error
  </behavior>
  <action>
Create `src/lib/types.ts` with the following complete type hierarchy. This is the canonical schema — later phases do NOT change field names or structure, they only fill in values.

```typescript
// src/lib/types.ts

export type GoalType = 'retirement' | 'purchase' | 'education' | 'legacy';

export interface RetirementGoal {
  type: 'retirement';
  targetRetirementAge: number;
  desiredAnnualIncome: number; // in today's dollars
  yearsInRetirement: number;   // e.g., 30
}

export interface PurchaseGoal {
  type: 'purchase';
  description: string;
  targetAmount: number;
  targetYear: number;
}

export interface EducationGoal {
  type: 'education';
  beneficiary: string;          // e.g., "child 1"
  targetAmount: number;
  targetYear: number;
}

export interface LegacyGoal {
  type: 'legacy';
  description: string;
  targetAmount: number;
}

export type Goal = RetirementGoal | PurchaseGoal | EducationGoal | LegacyGoal;

export interface Income {
  salary: number;              // annual gross salary
  otherAnnualIncome: number;   // rental, freelance, etc.
  annualSavingsRate: number;   // 0–1 (e.g., 0.15 = 15%)
}

export interface Expenses {
  monthlyEssential: number;    // rent, utilities, food
  monthlyDiscretionary: number; // dining, entertainment
  monthlyDebtPayments: number;
}

export interface Assets {
  checkingAndSavings: number;
  retirementAccounts: number;  // 401k, IRA
  taxableInvestments: number;
  realEstateEquity: number;
  otherAssets: number;
}

export interface Liabilities {
  mortgageBalance: number;
  studentLoanBalance: number;
  autoLoanBalance: number;
  creditCardBalance: number;
  otherDebt: number;
}

export type RiskToleranceLevel = 'conservative' | 'moderate' | 'aggressive';

export interface RiskTolerance {
  score: number;                    // 1–10 from questionnaire
  level: RiskToleranceLevel | null; // derived from score; null until answered
  answers: Record<string, number>;  // questionId -> answer value
}

export interface GoalResult {
  goalIndex: number;
  goalType: GoalType;
  probabilityScore: number;         // 0–1
  fundingGap: number;               // positive = shortfall in today's dollars
  targetAmount: number;
}

export interface SimulationResults {
  overallProbability: number;       // 0–1
  goalResults: GoalResult[];
  runCount: number;                 // number of Monte Carlo iterations
  assumptions: {
    inflationRate: number;          // e.g., 0.03
    realReturnMean: number;         // e.g., 0.07
    realReturnStdDev: number;       // e.g., 0.12
  };
  ranAt: string;                    // ISO timestamp
}

export interface PlanMetadata {
  createdAt: string;               // ISO timestamp
  updatedAt: string;               // ISO timestamp
  version: number;                 // increment on each save
}

export interface FinancialPlan {
  metadata: PlanMetadata;
  income: Income;
  expenses: Expenses;
  assets: Assets;
  liabilities: Liabilities;
  goals: Goal[];
  riskTolerance: RiskTolerance;
  simulationResults: SimulationResults | null;
}
```

Create `src/lib/planDefaults.ts` with a factory function:

```typescript
// src/lib/planDefaults.ts
import type { FinancialPlan } from './types';

export function createEmptyPlan(): FinancialPlan {
  const now = new Date().toISOString();
  return {
    metadata: {
      createdAt: now,
      updatedAt: now,
      version: 1,
    },
    income: {
      salary: 0,
      otherAnnualIncome: 0,
      annualSavingsRate: 0,
    },
    expenses: {
      monthlyEssential: 0,
      monthlyDiscretionary: 0,
      monthlyDebtPayments: 0,
    },
    assets: {
      checkingAndSavings: 0,
      retirementAccounts: 0,
      taxableInvestments: 0,
      realEstateEquity: 0,
      otherAssets: 0,
    },
    liabilities: {
      mortgageBalance: 0,
      studentLoanBalance: 0,
      autoLoanBalance: 0,
      creditCardBalance: 0,
      otherDebt: 0,
    },
    goals: [],
    riskTolerance: {
      score: 0,
      level: null,
      answers: {},
    },
    simulationResults: null,
  };
}
```

Write tests in `src/lib/__tests__/planDefaults.test.ts` using Jest (which Next.js includes via `@jest/globals` or via the built-in test runner). If the project uses Vitest instead, adjust imports accordingly. Test the behavior cases listed in the `<behavior>` block above.
  </action>
  <verify>
    <automated>cd /home/john/Projects/MonteCarloFinancialPlanning && npx tsc --noEmit 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/types.ts` exports `FinancialPlan`, `Goal`, `RetirementGoal`, `PurchaseGoal`, `EducationGoal`, `LegacyGoal`, `Assets`, `Liabilities`, `RiskTolerance`, `SimulationResults`, `GoalResult`, `PlanMetadata`
    - `src/lib/planDefaults.ts` exports `createEmptyPlan`
    - `grep -c "export interface\|export type" src/lib/types.ts` returns 12 or more
    - `npx tsc --noEmit` exits with code 0 (no TypeScript errors)
    - `src/lib/__tests__/planDefaults.test.ts` exists and test for `createEmptyPlan().simulationResults === null` passes
  </acceptance_criteria>
  <done>Complete FinancialPlan type hierarchy defined covering all data for Phases 2-4. createEmptyPlan() factory returns a fully-populated zero-value plan. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 3: Build app shell with layout, top nav, sidebar, and dashboard placeholder</name>
  <files>
    src/app/layout.tsx,
    src/app/page.tsx,
    src/components/AppShell.tsx,
    src/components/TopNav.tsx,
    src/components/Sidebar.tsx
  </files>
  <read_first>
    - /home/john/Projects/MonteCarloFinancialPlanning/src/app/layout.tsx (current state after scaffold)
    - /home/john/Projects/MonteCarloFinancialPlanning/src/app/page.tsx (current state after scaffold)
    - /home/john/Projects/MonteCarloFinancialPlanning/.planning/phases/01-foundation/01-CONTEXT.md (D-08 app shell decision, D-09 Tailwind)
  </read_first>
  <action>
Create the following components. Use Tailwind utility classes throughout — no CSS modules, no inline styles.

**`src/components/TopNav.tsx`**
```tsx
import Link from 'next/link';

export function TopNav() {
  return (
    <header className="flex h-14 items-center border-b border-gray-200 bg-white px-6">
      <Link href="/" className="text-lg font-semibold text-gray-900">
        Monte Carlo Planner
      </Link>
      <nav className="ml-auto flex items-center gap-4">
        <span className="text-sm text-gray-500">Single-user prototype</span>
      </nav>
    </header>
  );
}
```

**`src/components/Sidebar.tsx`**
```tsx
import Link from 'next/link';

const navItems = [
  { label: 'Dashboard', href: '/' },
  { label: 'Interview', href: '/interview' },
  { label: 'Simulation', href: '/simulation' },
  { label: 'Results', href: '/results' },
];

export function Sidebar() {
  return (
    <aside className="flex w-56 flex-col border-r border-gray-200 bg-gray-50 px-4 py-6">
      <nav className="flex flex-col gap-1">
        {navItems.map((item) => (
          <Link
            key={item.href}
            href={item.href}
            className="rounded-md px-3 py-2 text-sm text-gray-700 hover:bg-gray-100 hover:text-gray-900"
          >
            {item.label}
          </Link>
        ))}
      </nav>
    </aside>
  );
}
```

**`src/components/AppShell.tsx`**
```tsx
import { TopNav } from './TopNav';
import { Sidebar } from './Sidebar';

interface AppShellProps {
  children: React.ReactNode;
}

export function AppShell({ children }: AppShellProps) {
  return (
    <div className="flex h-screen flex-col">
      <TopNav />
      <div className="flex flex-1 overflow-hidden">
        <Sidebar />
        <main className="flex-1 overflow-auto bg-white p-6">{children}</main>
      </div>
    </div>
  );
}
```

**`src/app/layout.tsx`** — Replace the scaffolded root layout:
```tsx
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import { AppShell } from '@/components/AppShell';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Monte Carlo Financial Planner',
  description: 'Financial planning with Monte Carlo simulation',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <AppShell>{children}</AppShell>
      </body>
    </html>
  );
}
```

**`src/app/page.tsx`** — Replace the scaffolded home page:
```tsx
export default function DashboardPage() {
  return (
    <div className="max-w-2xl">
      <h1 className="text-2xl font-bold text-gray-900">Financial Plan Dashboard</h1>
      <p className="mt-2 text-gray-500">
        Complete the interview to generate your Monte Carlo probability score.
      </p>
      <div className="mt-6 rounded-lg border border-gray-200 bg-gray-50 p-6">
        <p className="text-sm text-gray-500">No plan data yet. Start the interview to begin.</p>
      </div>
    </div>
  );
}
```
  </action>
  <verify>
    <automated>cd /home/john/Projects/MonteCarloFinancialPlanning && npm run build 2>&1 | grep -E "error|Error|warning|compiled" | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/AppShell.tsx` exists and contains `TopNav` and `Sidebar` imports
    - `src/components/TopNav.tsx` exists and contains `Monte Carlo Planner`
    - `src/components/Sidebar.tsx` exists and contains hrefs for `/interview`, `/simulation`, `/results`
    - `src/app/layout.tsx` imports `AppShell` from `@/components/AppShell`
    - `src/app/page.tsx` contains `Financial Plan Dashboard`
    - `npm run build` exits with code 0
    - `npm run dev` starts without errors (verify by running for 5 seconds and checking for READY output)
  </acceptance_criteria>
  <done>App shell renders in browser at localhost:3000 showing top nav with "Monte Carlo Planner", left sidebar with Dashboard/Interview/Simulation/Results nav links, and a main content area with the dashboard placeholder. Build compiles without errors.</done>
</task>

</tasks>

<verification>
After all three tasks complete, run the following checks:

1. `cd /home/john/Projects/MonteCarloFinancialPlanning && npm run build` — must exit 0
2. `npx tsc --noEmit` — must exit 0 (no type errors)
3. `grep -r "export interface FinancialPlan" src/lib/types.ts` — must return a match
4. `grep "createEmptyPlan" src/lib/planDefaults.ts` — must return a match
5. `grep "AppShell" src/app/layout.tsx` — must return a match
6. `cat .gitignore | grep "^data/"` — must return `data/`
</verification>

<success_criteria>
- `npm run dev` starts the Next.js dev server on port 3000 with no errors
- `npm run build` compiles cleanly with zero TypeScript or lint errors
- `src/lib/types.ts` defines `FinancialPlan` with all sub-types covering income, expenses, assets, liabilities, goals (retirement/purchase/education/legacy), risk tolerance, and simulation results
- `src/lib/planDefaults.ts` exports `createEmptyPlan()` returning a valid zero-value `FinancialPlan`
- Visiting localhost:3000 renders the AppShell layout with TopNav + Sidebar + dashboard placeholder
- `data/` is in `.gitignore`
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` following the summary template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
