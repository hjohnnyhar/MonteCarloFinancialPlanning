---
phase: 02-interview-wizard
plan: 04
type: execute
wave: 4
depends_on: ["02-02", "02-03"]
files_modified:
  - src/components/interview/steps/RiskToleranceStep.tsx
  - src/components/interview/steps/ReviewStep.tsx
  - src/app/interview/page.tsx
autonomous: false
requirements: [INT-05, INT-06, INT-07]

must_haves:
  truths:
    - "Risk tolerance step shows all 4 questions on one screen simultaneously (D-07)"
    - "Each question has radio buttons with 3 labeled answer options (D-08)"
    - "After answering all questions, derived risk level is shown with explanation (D-09)"
    - "Review step shows read-only summary of all sections with Edit buttons (INT-07)"
    - "Edit buttons in Review navigate back to the corresponding wizard step index"
    - "Review step is only reachable when all prior steps have valid data (D-06)"
    - "Final CTA on Review is 'Save & Run Simulation'"
  artifacts:
    - path: "src/components/interview/steps/RiskToleranceStep.tsx"
      provides: "4-question risk questionnaire with score derivation"
      exports: ["RiskToleranceStep"]
    - path: "src/components/interview/steps/ReviewStep.tsx"
      provides: "Read-only plan summary with per-section Edit links"
      exports: ["ReviewStep"]
  key_links:
    - from: "src/components/interview/steps/RiskToleranceStep.tsx"
      to: "src/lib/riskToleranceQuestions.ts"
      via: "imports RISK_QUESTIONS, scoreFromAnswers, deriveRiskLevel"
      pattern: "import.*RISK_QUESTIONS.*scoreFromAnswers.*deriveRiskLevel"
    - from: "src/components/interview/steps/ReviewStep.tsx"
      to: "src/lib/wizardStore.ts"
      via: "wizardStore.setStepIndex for Edit navigation"
      pattern: "wizardStore.*setStepIndex"
---

<objective>
Build the Risk Tolerance questionnaire step and the Review & Confirm summary step, completing the full interview wizard flow.

Purpose: Capture the user's risk profile via a scored questionnaire and provide a summary view where users can review all entered data and navigate back to edit any section.
Output: RiskToleranceStep, ReviewStep, and complete wizard flow from step 1 through review.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-interview-wizard/02-CONTEXT.md
@.planning/phases/02-interview-wizard/02-RESEARCH.md
@.planning/phases/02-interview-wizard/02-UI-SPEC.md
@.planning/phases/02-interview-wizard/02-02-SUMMARY.md
@.planning/phases/02-interview-wizard/02-03-SUMMARY.md

@src/lib/types.ts
@src/lib/wizardSchemas.ts
@src/lib/riskToleranceQuestions.ts
@src/lib/wizardSteps.ts
@src/lib/wizardStore.ts
@src/hooks/usePlan.ts
@src/app/interview/page.tsx
@src/components/interview/steps/IncomeExpensesStep.tsx

<interfaces>
From src/lib/riskToleranceQuestions.ts:
```typescript
export interface RiskQuestion { id: string; text: string; options: { label: string; value: number }[]; }
export const RISK_QUESTIONS: RiskQuestion[]; // 4 questions
export function scoreFromAnswers(answers: Record<string, number>): number; // returns 1-10
export function deriveRiskLevel(score: number): 'conservative' | 'moderate' | 'aggressive';
```

From src/lib/types.ts:
```typescript
export interface RiskTolerance { score: number; level: RiskToleranceLevel | null; answers: Record<string, number>; }
export type RiskToleranceLevel = 'conservative' | 'moderate' | 'aggressive';
export interface FinancialPlan { metadata: PlanMetadata; income: Income; expenses: Expenses; assets: Assets; liabilities: Liabilities; goals: Goal[]; riskTolerance: RiskTolerance; simulationResults: SimulationResults | null; }
```

From src/lib/wizardSchemas.ts:
```typescript
export const riskToleranceSchema: z.ZodObject<{ answers: z.ZodRecord }>;
```

From src/lib/wizardStore.ts:
```typescript
export const wizardStore: { setStepIndex: (i: number) => void; ... };
```

From src/lib/wizardSteps.ts:
```typescript
export const WIZARD_STEPS: WizardStepConfig[]; // indices: 0=income, 1=assets, 2=goals, 3=risk, 4=review
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RiskToleranceStep and ReviewStep, wire into InterviewPage</name>
  <files>src/components/interview/steps/RiskToleranceStep.tsx, src/components/interview/steps/ReviewStep.tsx, src/app/interview/page.tsx</files>
  <read_first>
    - src/lib/riskToleranceQuestions.ts
    - src/lib/types.ts
    - src/lib/wizardSchemas.ts
    - src/lib/wizardSteps.ts
    - src/lib/wizardStore.ts
    - src/app/interview/page.tsx
    - src/components/interview/steps/IncomeExpensesStep.tsx
    - src/hooks/usePlan.ts
    - .planning/phases/02-interview-wizard/02-UI-SPEC.md
    - .planning/phases/02-interview-wizard/02-CONTEXT.md
  </read_first>
  <action>
1. **Create `src/components/interview/steps/RiskToleranceStep.tsx`:**

   Props: `{ plan: FinancialPlan; onComplete: (data: Partial<FinancialPlan>) => Promise<void>; onBack: () => void; }`

   Per D-07: All 4 questions shown on one screen. Per D-08: Radio buttons per question. Per D-09: Show derived risk level after answering.

   **State:**
   ```typescript
   const [answers, setAnswers] = useState<Record<string, number>>(plan.riskTolerance.answers || {});
   ```

   Initialize answers from `plan.riskTolerance.answers` (pre-population for INT-08).

   **Render each question** using RISK_QUESTIONS array:
   ```typescript
   import { RISK_QUESTIONS, scoreFromAnswers, deriveRiskLevel } from '@/lib/riskToleranceQuestions';
   ```

   For each question, render a `<fieldset>` per UI-SPEC RadioGroup pattern:
   ```html
   <fieldset className="flex flex-col gap-3">
     <legend className="text-sm font-normal text-gray-700">{question.text}</legend>
     {question.options.map(option => (
       <label key={option.value} className="flex items-center gap-3 cursor-pointer">
         <input
           type="radio"
           name={question.id}
           value={option.value}
           checked={answers[question.id] === option.value}
           onChange={() => setAnswers(prev => ({ ...prev, [question.id]: option.value }))}
           className="h-4 w-4 border-gray-300 text-blue-600 focus:ring-blue-500"
         />
         <span className="text-base text-gray-900">{option.label}</span>
       </label>
     ))}
   </fieldset>
   ```

   Stack questions with `<div className="space-y-8">` (32px between questions).

   **Derived risk display** (D-09): Show only when ALL questions are answered:
   ```typescript
   const allAnswered = RISK_QUESTIONS.every(q => answers[q.id] !== undefined);
   const score = allAnswered ? scoreFromAnswers(answers) : null;
   const level = score !== null ? deriveRiskLevel(score) : null;
   ```

   When `allAnswered && level`:
   ```html
   <div className="mt-6 rounded-lg border border-gray-200 bg-gray-50 p-4">
     <p className="text-sm font-semibold text-gray-900">
       Based on your answers: <span className="capitalize">{level}</span> risk tolerance
     </p>
     <p className="mt-1 text-sm text-gray-500">
       {level === 'conservative' ? 'Focus on capital preservation with lower-risk investments.' :
        level === 'moderate' ? 'Balanced mix of stocks and bonds for growth with moderate risk.' :
        'Growth-focused portfolio accepting higher short-term volatility.'}
     </p>
     <p className="mt-1 text-sm text-gray-500">Score: {score}/10</p>
   </div>
   ```

   **handleNext:**
   ```typescript
   const handleNext = async () => {
     const score = scoreFromAnswers(answers);
     const level = deriveRiskLevel(score);
     await onComplete({
       riskTolerance: { answers, score, level },
     });
   };
   ```

   Navigation buttons: Back + Next at bottom (same pattern as other steps).

2. **Create `src/components/interview/steps/ReviewStep.tsx`:**

   Props: `{ plan: FinancialPlan; onComplete: () => Promise<void>; goToStep: (index: number) => void; }`

   Per D-11/INT-07: Edit buttons navigate back to the relevant step. Per D-06: Review is only reachable when all prior steps have valid data.

   The Review step renders read-only summary cards for each section with an "Edit" button.

   Import `wizardStore` from `@/lib/wizardStore` for the `goToStep` calls (or use the prop).

   **Layout:** Stack of summary cards using UI-SPEC Card/Panel pattern.

   **Section 1: Income & Expenses** (Edit navigates to step 0):
   ```html
   <div className="rounded-lg border border-gray-200 bg-gray-50 p-4">
     <div className="flex items-center justify-between">
       <h3 className="text-sm font-semibold text-gray-900">Income & Expenses</h3>
       <button type="button" onClick={() => goToStep(0)} className="text-sm font-semibold text-blue-600 hover:text-blue-700">Edit</button>
     </div>
     <dl className="mt-3 grid grid-cols-2 gap-2 text-sm">
       <div><dt className="text-gray-500">Annual Salary</dt><dd className="text-gray-900">${plan.income.salary.toLocaleString()}</dd></div>
       <div><dt className="text-gray-500">Other Income</dt><dd className="text-gray-900">${plan.income.otherAnnualIncome.toLocaleString()}</dd></div>
       <div><dt className="text-gray-500">Savings Rate</dt><dd className="text-gray-900">{(plan.income.annualSavingsRate * 100).toFixed(0)}%</dd></div>
       <div><dt className="text-gray-500">Monthly Essential</dt><dd className="text-gray-900">${plan.expenses.monthlyEssential.toLocaleString()}</dd></div>
       <div><dt className="text-gray-500">Monthly Discretionary</dt><dd className="text-gray-900">${plan.expenses.monthlyDiscretionary.toLocaleString()}</dd></div>
       <div><dt className="text-gray-500">Monthly Debt</dt><dd className="text-gray-900">${plan.expenses.monthlyDebtPayments.toLocaleString()}</dd></div>
     </dl>
   </div>
   ```

   **Section 2: Assets & Liabilities** (Edit navigates to step 1):
   Similar card with all 5 asset fields and 5 liability fields. Show net worth calculation:
   ```typescript
   const totalAssets = plan.assets.checkingAndSavings + plan.assets.retirementAccounts + plan.assets.taxableInvestments + plan.assets.realEstateEquity + plan.assets.otherAssets;
   const totalLiabilities = plan.liabilities.mortgageBalance + plan.liabilities.studentLoanBalance + plan.liabilities.autoLoanBalance + plan.liabilities.creditCardBalance + plan.liabilities.otherDebt;
   const netWorth = totalAssets - totalLiabilities;
   ```
   Display: "Net Worth: ${netWorth.toLocaleString()}" as a summary line.

   **Section 3: Financial Goals** (Edit navigates to step 2):
   Card showing "{plan.goals.length} goals defined". List each goal with type and key detail:
   - Retirement: "Retire at {age} with ${amount}/yr"
   - Purchase: "{desc} - ${amount}"
   - Education: "{beneficiary} - ${amount}"
   - Legacy: "{desc} - ${amount}"

   **Section 4: Risk Tolerance** (Edit navigates to step 3):
   Card showing: "{level} (score: {score}/10)" or "Not completed" if score is 0 and level is null.

   **Final CTA button:**
   ```html
   <div className="mt-8 flex items-center justify-between border-t border-gray-200 pt-6">
     <button type="button" onClick={() => goToStep(3)} className="...secondary button...">Back</button>
     <button type="button" onClick={onComplete} className="rounded-md bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">
       Save &amp; Run Simulation
     </button>
   </div>
   ```

   The `onComplete` handler in the page should call `updatePlan({ metadata: { wizardStep: 5 } })` (marking wizard as complete) and optionally navigate to a simulation page (for now, just save — Phase 3 handles simulation).

3. **Update `src/app/interview/page.tsx`:**
   - Import RiskToleranceStep and ReviewStep
   - Render RiskToleranceStep at stepIndex 3
   - Render ReviewStep at stepIndex 4
   - Remove placeholder divs for steps 3 and 4
   - For ReviewStep, pass `goToStep` function that sets stepIndex AND updates wizardStore
   - Implement D-06 (Review only enabled when all steps valid): Add a `canAccessReview` check. Simplest implementation: check that plan has non-zero income OR expenses OR assets (at least some data entered). If the user tries to access step 4 (Review) via sidebar click when `canAccessReview` is false, redirect to the first incomplete step. For the prototype, a simple heuristic suffices: all steps before Review are "valid" if the wizard has been advanced past them at least once (i.e., `completedSteps` includes indices 0-3).

   The "Save & Run Simulation" button in ReviewStep: for now, have it call `updatePlan({ metadata: { wizardStep: 5 } })` and then `router.push('/')` to return to the dashboard. Phase 3 will add simulation triggering.
   ```typescript
   import { useRouter } from 'next/navigation';
   // In handleFinish:
   const handleFinish = async () => {
     await updatePlan({ metadata: { wizardStep: 5 } });
     router.push('/');
   };
   ```
  </action>
  <verify>
    <automated>cd /home/john/Projects/MonteCarloFinancialPlanning && npx tsc --noEmit 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - src/components/interview/steps/RiskToleranceStep.tsx exists and contains "export function RiskToleranceStep"
    - RiskToleranceStep.tsx contains "RISK_QUESTIONS" import
    - RiskToleranceStep.tsx contains "scoreFromAnswers" and "deriveRiskLevel" imports
    - RiskToleranceStep.tsx contains "<fieldset" (per UI-SPEC RadioGroup)
    - RiskToleranceStep.tsx contains "<legend" (accessibility)
    - RiskToleranceStep.tsx contains 'type="radio"' inputs
    - RiskToleranceStep.tsx contains "Based on your answers" (D-09 confirmation text)
    - RiskToleranceStep.tsx contains "conservative" and "moderate" and "aggressive" text
    - RiskToleranceStep.tsx contains "Score:" and "/10" display
    - src/components/interview/steps/ReviewStep.tsx exists and contains "export function ReviewStep"
    - ReviewStep.tsx contains "goToStep(0)" or "goToStep(1)" (edit navigation)
    - ReviewStep.tsx contains "Income & Expenses" and "Assets & Liabilities" and "Financial Goals" and "Risk Tolerance" section headings
    - ReviewStep.tsx contains "Save" (final CTA button)
    - ReviewStep.tsx contains "Edit" buttons (at least 4 instances)
    - ReviewStep.tsx contains "toLocaleString" (currency formatting)
    - ReviewStep.tsx contains "Net Worth" or "netWorth" calculation
    - src/app/interview/page.tsx contains "RiskToleranceStep" import
    - src/app/interview/page.tsx contains "ReviewStep" import
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>RiskToleranceStep renders 4 questions with radio buttons and derived risk display, ReviewStep shows full plan summary with Edit links and final CTA, wizard flow is complete from step 1 through review</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete wizard flow end-to-end</name>
  <files>src/app/interview/page.tsx</files>
  <read_first>
    - src/app/interview/page.tsx
    - data/plan.json
  </read_first>
  <action>
Human verification of the complete interview wizard flow. The dev server should already be running from Task 1 verification. Walk through all verification steps below to confirm the wizard works end-to-end. No code changes are expected in this task — this is purely a visual and functional verification checkpoint.
  </action>
  <what-built>Complete 5-step interview wizard at /interview with Income and Expenses, Assets and Liabilities, Financial Goals (tabbed with add/edit/remove), Risk Tolerance questionnaire, and Review and Confirm steps. Sidebar shows wizard progress. Dashboard has Start Interview button. All data persists to plan.json.</what-built>
  <how-to-verify>
    1. Start dev server: `cd /home/john/Projects/MonteCarloFinancialPlanning && npm run dev` (runs on port 3001)
    2. Open http://localhost:3001 — verify "Start Interview" button appears on dashboard
    3. Click "Start Interview" — verify /interview loads with step 1 (Income and Expenses)
    4. Verify sidebar shows wizard step list with step 1 active (blue), steps 2-5 grayed out
    5. Enter some income values (e.g., salary: 120000), click Next
    6. Verify step 2 (Assets and Liabilities) loads, sidebar updates (step 1 completed with green check, step 2 active)
    7. Enter some asset values, click Next
    8. Verify step 3 (Goals) loads with 4 tabs: Retirement, Purchases, Education, Legacy
    9. Add a retirement goal (age 65, $80000/yr, 30 years), verify it appears in the list
    10. Add a purchase goal, verify it appears in the Purchases tab
    11. Click Next to advance to step 4 (Risk Tolerance)
    12. Verify all 4 questions display simultaneously with radio buttons
    13. Answer all questions — verify risk level and score appear below questions
    14. Click Next to reach Review step
    15. Verify Review shows all entered data with Edit buttons for each section
    16. Click an Edit button — verify it navigates back to that step with data preserved
    17. Navigate back to Review, click "Save and Run Simulation" — verify redirect to dashboard
    18. Close browser tab, reopen http://localhost:3001/interview — verify wizard resumes at correct step
    19. Check data/plan.json — verify all entered data persisted correctly
  </how-to-verify>
  <verify>
    <automated>cd /home/john/Projects/MonteCarloFinancialPlanning && npx tsc --noEmit && npm test -- --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - User confirms wizard flow works end-to-end through all 5 steps
    - All steps render and accept data without console errors
    - Sidebar reflects wizard progress (active, completed, future states)
    - Data persists in data/plan.json with correct structure
    - Resume on reload navigates to the saved wizard step
  </acceptance_criteria>
  <done>User has approved the complete wizard flow or issues have been identified and addressed</done>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero type errors
2. Full wizard flow: Dashboard -> Start Interview -> 5 steps -> back to dashboard
3. Risk tolerance: all 4 questions render, answering shows derived level
4. Review: all sections display with correct data, Edit links navigate correctly
5. Resume: close and reopen /interview resumes at saved step
6. Sidebar: reflects wizard progress throughout
</verification>

<success_criteria>
- Risk tolerance captures answers via radio buttons (D-07, D-08), shows derived level (D-09)
- Review step shows read-only summary of ALL entered data (INT-07)
- Edit buttons in Review navigate to correct wizard step for editing
- "Save & Run Simulation" saves final state and returns to dashboard
- Complete end-to-end wizard flow works without errors
- All data persists across page refreshes (INT-06)
- Pre-populated fields show existing data when re-entering wizard (INT-08)
</success_criteria>

<output>
After completion, create `.planning/phases/02-interview-wizard/02-04-SUMMARY.md`
</output>
